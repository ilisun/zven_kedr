<div class="container">
  <h1 class="center"><span class="light">Редактирование</span> товара</h1>
  <hr class="divider">
  <div class="row">
    <%= form_for @product do |f| %>
      <div class="col-xs-12 col-sm-4">
        <%= f.label :Название %> <br>
        <%= f.text_field :name, class: 'form-control  form-control--contact' %> <br>
        <%= f.label :Цена %> <br>
        <%= f.text_field :price, class: 'form-control  form-control--contact' %> <br>
        <%= f.label :Описание %> <br>
        <%= f.text_area :description, class: 'form-control  form-control--contact  form-control--big', rows: 10 %> <br>
      </div>
      <div class="col-xs-12 col-sm-4">
        <%= f.label :Категории %> <br>
        <hr>
        <%= f.collection_check_boxes :category_ids, Category.all.order(:id), :id, :name do |b| %> <br>
          <div class="row">
            <div class="col-xs-1">
              <%= b.check_box(class: "check_box") %>
            </div>
            <div class="col-xs-10">
              <%= b.object.name %>
            </div>
          </div>
          <% if (b.object.id == 4) %>
            <hr>
          <% end %>
        <% end %>
        <hr>
      </div>
      <div class="col-xs-12 col-sm-4">
        <%= f.label :Фото %> <br>
        <hr>
        <%= image_tag @product.image.url(:medium) %> <br><br>
        <%= f.file_field :image %>
        <hr class="title__divider">
        <%= f.submit "Сохранить", class: "btn btn-primary--reverse-transition" %>
        <%= link_to 'Назад', products_path(:page => session[:npage]), class: "btn btn-info--transition" %><br>
      </div>
    <% end %>
  </div>
  <hr class="divider">
</div>